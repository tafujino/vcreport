directory: `<%= @results_dir.to_s %>`

page: <%= paging.current_page %> / <%= paging.total_page %>

<%= navigation_markdown_text(PREFIX, paging) %>

<%= table.markdown_text %>

<%= navigation_markdown_text(PREFIX, paging) %>
