daemon off;
events {}
http {
  server {
    listen       <%= port %> default;
    server_name  localhost;
    access_log   <%= access_log_path %>;

    location / {
      root   <%= report_dir %>;
      index  index.html;
    }
  }
}